name: Deploy to Firebase — Production

on:
  push:
    branches:
      - main

permissions:
  contents: read

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v5

      - name: Setup Node.js
        uses: actions/setup-node@v5
        with:
          node-version: '24'

      - name: Install dependencies
        run: npm install --legacy-peer-deps

      - name: Build for Firebase (root URL)
        env:
          VITE_FIREBASE_API_KEY: ${{ secrets.FIREBASE_API_KEY }}
          VITE_FIREBASE_AUTH_DOMAIN: "resitalaina-o-matai.firebaseapp.com"
          VITE_FIREBASE_PROJECT_ID: "resitalaina-o-matai"
          VITE_FIREBASE_STORAGE_BUCKET: "resitalaina-o-matai.firebasestorage.app"
          VITE_FIREBASE_MESSAGING_SENDER_ID: "1066492699678"
          VITE_FIREBASE_APP_ID: "1:1066492699678:web:e2e6e0859d192be347ba0f"
          VITE_RECAPTCHA_SITE_KEY: ${{ secrets.RECAPTCHA_SITE_KEY }}
        run: |
          npm run build

      - name: Install Firebase CLI
        run: npm install -g firebase-tools

      - name: Deploy to Firebase Hosting + Functions
        env:
          GOOGLE_APPLICATION_CREDENTIALS_JSON: ${{ secrets.FIREBASE_SERVICE_ACCOUNT }}
          RESEND_API_KEY_VALUE: ${{ secrets.RESEND_API_KEY }}
        run: |
          echo "$GOOGLE_APPLICATION_CREDENTIALS_JSON" > /tmp/service-account.json
          echo "RESEND_API_KEY=$RESEND_API_KEY_VALUE" > functions/.env
          export GOOGLE_APPLICATION_CREDENTIALS=/tmp/service-account.json
          cd functions && npm install && cd ..
          firebase functions:artifacts:setpolicy --project resitalaina-o-matai --location australia-southeast1 --force 2>/dev/null || true
          firebase deploy --only hosting:resitalaina-o-matai,functions,firestore:rules --project resitalaina-o-matai --non-interactive --force
