name: Deploy to GitHub Pages (DISABLED)

# Disabled — production is now served from smrs.mjca.gov.ws via Firebase Hosting
# GitHub Pages was the old deployment target and is no longer used
on:
  workflow_dispatch: # manual trigger only — disabled from auto-deploy

jobs:
  deploy-pages:
    if: false # never runs
    runs-on: ubuntu-latest
    steps:
      - run: echo "GitHub Pages deployment is disabled. Use Firebase Hosting (smrs.mjca.gov.ws)."
